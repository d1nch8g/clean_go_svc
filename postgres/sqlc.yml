version: 1
packages:
  - path: "gen/sqlc"
    name: "sqlc"
    engine: "postgresql"
    schema: "migrations/"
    queries: "sqlc.sql"
    emit_interface: true
    emit_prepared_queries: true
    sql_package: "pgx/v4"
